JEV_BASE_URL=http://127.0.0.1:8000
# Optional: leave empty to use the model loaded by the local server.
TYPESAFE_MODEL=
# Optional: only needed if local server authentication is enabled.
TYPESAFE_API_KEY=
